const users = {
    vs: {
        country: 'CO'
    },
    fabio: {
        country: 'PE'
    }
}

// console.log(users.vs.country);
console.log(users?.vars?.country); // validate if is in the data base